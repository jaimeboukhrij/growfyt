{
  "name": "growfit-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "clean": "turbo run clean",
    "type-check": "turbo run type-check",
    "prepare-deploy": "bash scripts/prepare-deploy.sh",
    "verify-domains": "bash scripts/verify-domains.sh"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "turbo": "^1.11.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
